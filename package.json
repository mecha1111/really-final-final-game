{
  "name": "really-final-final-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bash -c 'rm -rf dist && cp -R game dist' _",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.4.2"
  }
}
